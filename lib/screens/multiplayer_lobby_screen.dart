import 'package:flutter/material.dart';

class MultiplayerLobbyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Multiplayer Lobby'),
      ),
      body: Center(
        child: Text('Multiplayer Lobby Screen'),
      ),
    );
  }
}
